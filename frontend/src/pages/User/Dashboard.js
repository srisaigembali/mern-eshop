import React from 'react';
import Layout from '../../components/Layout';

const Dashboard = () => {
  return (
    <Layout>
      <div>Dashboard</div>
    </Layout>
  );
};

export default Dashboard;
